import React from 'react';
import ReactDOM from 'react-dom/client';
import './styles.css';

const todoList = <h1 class="title">Todo List</h1>;
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(todoList);
